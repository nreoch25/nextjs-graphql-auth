type Query {
  users: [User!]!
}

type Mutation {
  signupUser(username: String!, email: String!, password: String!): Token!
}

type User {
  _id: ID!
  username: String!
  email: String!
  password: String!
}

type Token {
  token: String!
}
