upstream client {
  server client:3000;
}
upstream graphql{
  server graphql:8000;
}
server {
  listen 80;
  location / {
    proxy_pass http://client;
  }
  location /graphql {
    proxy_pass http://graphql;
  }
}